# Backend (Express)

Simple Express API running on port `5000`.

## Endpoints

- `GET /api/health`

Response:

```json
{
  "status": "OK",
  "message": "Backend running"
}
```

## Run locally

```bash
npm install
npm run dev
```

Server URL: `http://localhost:5000`
